{
  "name": "matrix-rain",
  "version": "0.2.0",
  "private": true,
  "node": "",
  "dependencies": {
    "@emotion/core": "^11.0.0",
    "@emotion/css": "^11.10.0",
    "@emotion/react": "^11.10.4",
    "@use-it/interval": "^1.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "devDependenciess": {    
    "serve": "^14.0.1"
  },
  "scripts": {
    "preview": "react-scripts build && serve -s build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
